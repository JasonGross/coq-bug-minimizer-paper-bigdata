This is already fixed in `master`, so we need an 8.8-specific fix.
@Zimmi48 has a fix been backported?
The fix is in #7143 I think.
Yep, sorry, I forgot to close. GitHub auto-close doesn't work when we don't merge in `master` (because they don't distinguish feature branches and stable branches).
