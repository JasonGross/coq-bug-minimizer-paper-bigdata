Oh, I see, the issue here is that the command used to invoke `coq_makefile` is hard-coded in the Makefile.  So this is only an issue when trying to switch between versions of Coq without deleting the generated file in between...
