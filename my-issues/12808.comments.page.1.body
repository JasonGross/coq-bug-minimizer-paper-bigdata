`Ltac2 Notation x(tactic) "+++" y(tactic) : 999 := x; y.` is accepted without complaint.  That seems like a bug.

I take it that the 6 is the precedence for the notation, right?  What about right and left associativity and the other things defined in `syntax_modifier`?
